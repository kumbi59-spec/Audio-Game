# Expo's Metro bundler requires packages to be resolvable from the project
# root node_modules (flat layout). pnpm's default strict layout puts packages
# only in node_modules/.pnpm, which Metro cannot traverse. shamefully-hoist
# replicates the npm/yarn flat layout so Metro can find all transitive deps.
shamefully-hoist=true
